//make sure to have lowercase so that it passes through
export type Movie = {
    movieId: number;
    category: string;
    title: string;
    year: number;
    director: string;
    rating: string;
    edited: string;
    lentTo: string;
    notes: string;
  };